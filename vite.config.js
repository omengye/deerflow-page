import { defineConfig } from 'vite'

export default defineConfig({
  // 如果部署到 GitHub Pages，请确保 base 路径与您的 GitHub 仓库名称（区分大小写）一致
  // 访问地址为 https://omengye.github.io/deerflow-page/
  base: '/deerflow-page/',
})
